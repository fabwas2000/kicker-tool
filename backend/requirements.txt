fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
pydantic==2.10.3
# Postgres-Treiber fuer den Cloud-Betrieb (Neon). Lokal mit SQLite wird er
# nicht gebraucht, stoert dort aber auch nicht.
psycopg[binary]==3.2.3
